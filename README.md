# Open Repository for API Specs
A library of API specs built from ground up, specifically for the use in AI.

# Credits
Each API spec directory has a source.json file which mentions the source, if known.
