{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "openapi": {
      "type": "object",
      "description": "metadata for the openapi spec",
      "properties": {
        "source": {
          "type": "string",
          "description": "The source URL of the OpenAPI specification. The URL must be publicly accessible and should not require any authentication or additional parameters. It should support GET requests."
        }
      }
    },
    "provider": {
      "type": "string",
      "description": "The provider that this API spec belongs to. Refer to https://docs.nango.dev/integrations/all/accelo for a list of supported providers."
    },
    "title": {
      "type": "string",
      "description": "The title of the API spec"
    }
  },
  "required": [
    "provider",
    "title"
  ]
}
